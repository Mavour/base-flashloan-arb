{
  "name": "base-flashloan-arb",
  "version": "1.0.0",
  "description": "Base network flashloan arbitrage bot — zero capital",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network base",
    "deploy:test": "hardhat run scripts/deploy.ts --network base-sepolia",
    "withdraw": "hardhat run scripts/withdraw.ts --network base",
    "check": "tsx scripts/checkOpportunity.ts",
    "bot": "tsx src/index.ts",
    "test": "hardhat test"
  },
  "dependencies": {
    "@aave/core-v3": "^1.19.3",
    "@openzeppelin/contracts": "^5.6.1",
    "axios": "^1.6.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.0.0",
    "hardhat": "^2.22.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
